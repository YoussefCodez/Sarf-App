abstract class NotificationConfig {
  // Morning Notification
  static int morningHour = 8;
  static int morningMinute = 0;

  // Evening Notification
  static int eveningHour = 20;
  static int eveningMinute = 0;

  // History Configuration
  static bool saveScheduledToHistory = true;
}
